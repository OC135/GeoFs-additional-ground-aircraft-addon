(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34753273483045;   // 経度
            const lat = 35.73895052401542;    // 緯度
            const alt = 138.1426816666042;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3471583916787;   // 経度
            const lat = 35.74074117261999;    // 緯度
            const alt = 138.1415313118002;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34698490561186;   // 経度
            const lat = 35.74163466044347;    // 緯度
            const alt = 138.14466550268855;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34707762087774;   // 経度
            const lat = 35.74119415864821;    // 緯度
            const alt = 138.14368261191387;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34673741175143;   // 経度
            const lat = 35.74283613767158;    // 緯度
            const alt = 138.1456159498946;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34654656881952;   // 経度
            const lat = 35.74372202301425;    // 緯度
            const alt = 138.14221504284768;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34647445;   // 経度
            const lat = 35.74415815;    // 緯度
            const alt = 138.142;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34639419489204;   // 経度
            const lat = 35.7445922217416;    // 緯度
            const alt = 138.14062377372423;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34619902706854;   // 経度
            const lat = 35.74547102360647;    // 緯度
            const alt = 138.14057151953168;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc130a400p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34331304864685;   // 経度
            const lat = 35.75497977787556;    // 緯度
            const alt = 138.64876961716536;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okocks748fp13.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34396819827424;   // 経度
            const lat = 35.75302798981193;    // 緯度
            const alt = 138.65259384460643;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okogti748fp12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34384979394255;   // 経度
            const lat = 35.753719299203524;    // 緯度
            const alt = 138.66263648675272;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okogti748fp12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34563936051637;   // 経度
            const lat = 35.74744396391059;    // 緯度
            const alt = 138.07734332777864;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okok35rk35rp16.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3455084276229;   // 経度
            const lat = 35.748221934023675;    // 緯度
            const alt = 138.07676363456886;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okok35rk35rp16.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34533449;   // 経度
            const lat = 35.74902223;    // 緯度
            const alt = 138.078;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okok35rk35rp16.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34517671436205;   // 経度
            const lat = 35.749821569456664;    // 緯度
            const alt = 138.07428282229313;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okok35rk35rp16.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34380134916148;   // 経度
            const lat = 35.754411404224165;    // 緯度
            const alt = 138.65757699536636;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okooae772p12.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34471781650254;   // 経度
            const lat = 35.74581117341237;    // 緯度
            const alt = 135.9441927456243;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okov22a609p14.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3445879028892;   // 経度
            const lat = 35.74643041415108;    // 緯度
            const alt = 135.94388389899422;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okov22a609p14.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3444480295722;   // 経度
            const lat = 35.747182026144216;    // 緯度
            const alt = 135.9434901829869;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okov22a609p14.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3443105593809;   // 経度
            const lat = 35.74782786358971;    // 緯度
            const alt = 135.94226701308753;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okov22a609p14.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34527112193524;   // 経度
            const lat = 35.7506705092835;    // 緯度
            const alt = 138.07835710560093;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okodc10md11p18.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34375834447005;   // 経度
            const lat = 35.75051329815617;    // 緯度
            const alt = 138.07209514289926;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okodc10md11p19.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34505104446714;   // 経度
            const lat = 35.75171930908526;    // 緯度
            const alt = 138.076308434892;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okodc10md11p18.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34351906494675;   // 経度
            const lat = 35.75161178465126;    // 緯度
            const alt = 138.07940430535072;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okodc10md11p19.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.3457827361472;   // 経度
            const lat = 35.74668510009272;    // 緯度
            const alt = 138.07645081922135;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okok35rk35rp16.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();

(function() {
    // GeoFS と Cesium の読み込みを待つ
    const wait = setInterval(() => {
        if (window.geofs?.api?.viewer && window.Cesium) {
            clearInterval(wait);

            const viewer = geofs.api.viewer;
            const Cesium = window.Cesium;

            // ★ ここを書き換えるだけ ★
            const lon = 139.34647747785826;   // 経度
            const lat = 35.738701716444574;    // 緯度
            const alt = 136.32486948214006;           // 高度[m]
            const modelUrl = "https://OC135.github.io/test6/okoc12e120p15.glb"; // GLBモデルのURL

            viewer.entities.add({
                position: Cesium.Cartesian3.fromDegrees(lon, lat, alt),
                model: {
                    uri: modelUrl,
                    scale: 1.0,
                    minimumPixelSize: 32
                }
            });

            console.log("モデルを配置しました:", lat, lon, alt);
        }
    }, 300);
})();
